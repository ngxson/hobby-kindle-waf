{
  "name": "com.ngxson.chess",
  "waf_application_name": "WAF Chess Game",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "chess.js": "^0.13.1",
    "js-chess-engine": "^1.0.2",
    "memoizee": "^0.4.15",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "prestart": "node scripts/generate-config.js",
    "start": "react-scripts start",
    "prebuild": "node scripts/generate-config.js",
    "build": "react-scripts build",
    "postbuild": "node scripts/bundle-kual-app.js",
    "serve": "serve -s build -l 4000"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      "ie 7"
    ],
    "development": [
      "ie 7"
    ]
  },
  "homepage": "."
}
